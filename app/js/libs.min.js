'use strict';

// bubbly-buton animate
var animateButton = function animateButton(e) {

    e.preventDefault;
    //reset animation
    e.target.classList.remove('animate');

    e.target.classList.add('animate');
    setTimeout(function () {
        e.target.classList.remove('animate');
    }, 700);
};

var bubblyButtons = document.getElementsByClassName("bubbly-button");

for (var i = 0; i < bubblyButtons.length; i++) {
    bubblyButtons[i].addEventListener('click', animateButton, false);
}
for (var _i = 0; _i < bubblyButtons.length; _i++) {
    bubblyButtons[_i].addEventListener('mouseover', animateButton, false);
}

var writeUs = document.getElementsByClassName('call-form'),
    formWrapper = document.getElementById('form-wrapper'),
    formContact = document.getElementById('form-contacts'),
    brand = document.getElementById('brand'),
    formClose = document.getElementsByClassName('close-form_wrapp'),
    burger = document.getElementsByClassName('burger'),
    burgerPlank = document.getElementsByClassName('burger-plank'),
    nav = document.getElementsByClassName('nav'),
    navigate = document.getElementById('navigate'),
    menu = document.getElementsByClassName('menu');

var callForm = function callForm(e) {
    e.preventDefault;
    e.target.classList.remove('visible_form');

    formWrapper.classList.add('visible_form');
    formContact.classList.add('visible_form');
};

for (var _i2 = 0; _i2 < writeUs.length; _i2++) {
    writeUs[_i2].addEventListener('click', callForm, false);
}

var closeForm = function closeForm(e) {
    e.preventDefault;
    formWrapper.classList.remove('visible_form');
};

for (var _i3 = 0; _i3 < formClose.length; _i3++) {
    formClose[_i3].addEventListener('click', closeForm, false);
}

var _loop = function _loop(_i4) {
    var oneNav = nav[_i4];
    var callMenu = function callMenu(e) {
        e.preventDefault;
        brand.classList.toggle('visible_form');
        oneNav.classList.toggle('visible_menu');
        menu[0].classList.toggle('col-9');
        menu[0].classList.toggle('col-12');
        menu[0].classList.toggle('col-md-3');
        navigate.classList.toggle('height-max');
        burgerPlank[0].classList.toggle('close-menu');
        burger[0].classList.toggle('close-menu_top');
        for (var _i5 = 0; _i5 < burgerPlank.length; _i5++) {
            burger[_i5].addEventListener('click', callMenu, false);
        }
    };

    for (var _i6 = 0; _i6 < burger.length; _i6++) {
        burger[_i6].addEventListener('click', callMenu, false);
    }
};

for (var _i4 = 0; _i4 < nav.length; _i4++) {
    _loop(_i4);
};

var btnMain = document.getElementsByClassName('btn-main');